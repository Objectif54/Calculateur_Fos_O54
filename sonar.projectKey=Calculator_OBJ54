node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    def scannerHome = tool 'Scanner-Sonar-Obj54';
    withSonarQubeEnv() {
      sh "${scannerHome}/bin/Scanner-Sonar-Obj54"
    }
  }
}
